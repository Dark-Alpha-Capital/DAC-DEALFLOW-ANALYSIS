{
  "name": "api",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts --watch",
    "build": "bun build index.ts --outdir dist --target bun --external hono",
    "start": "bun dist/index.js"
  },
  
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.11.7",
    "@repo/rag-engine": "workspace:*"
  }
}
